package pl.epsi.glWrapper.buffers.gpu;

public class MappedGpuBuffer extends GpuBuffer {

    public MappedGpuBuffer(BufferTarget bufferTarget, BufferUsage bufferUsage) {
        super(bufferTarget, bufferUsage);
    }
    
}
